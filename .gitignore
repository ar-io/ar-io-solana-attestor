node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store

# Workspace build output (redundant with dist/ above, kept explicit).
packages/*/dist/

# staging/rehearsal run artifacts (outputs, not source)
packages/claims/scripts/rehearsal-out/
